# API Reference

The main API endpoint is `/api/v1`. See below for details.

## Endpoints

- `GET /api/v1/items` — List all items
- `POST /api/v1/items` — Create an item
- `GET /api/v1/items/:id` — Get an item
